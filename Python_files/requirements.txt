# ── Audio capture & VAD ───────────────────────────────────────────────────────
sounddevice>=0.4.6
webrtcvad-wheels>=2.0.14       # pre-built wheels for Windows (replaces raw webrtcvad)
numpy>=1.24.0

# ── Speech recognition ────────────────────────────────────────────────────────
faster-whisper>=1.0.0

# ── Intent classification — Phi-2 + LoRA ─────────────────────────────────────
torch>=2.2.0                   # install the CUDA build manually if needed:
                               #   pip install torch --index-url https://download.pytorch.org/whl/cu121
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.29.0             # required by transformers for device_map / mixed precision
bitsandbytes>=0.43.0           # 4-bit NF4 quantization — install Windows build:
                               #   pip install bitsandbytes --index-url https://huggingface.github.io/bitsandbytes-windows-webui

# ── UI automation ─────────────────────────────────────────────────────────────
pyautogui>=0.9.54
Pillow>=10.0.0                 # PIL / ImageGrab
pywinauto>=0.6.8               # Windows UI automation — accessibility tree click
pywin32>=306                   # win32gui.GetForegroundWindow() for active-window UIA